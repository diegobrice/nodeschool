let input = ["R3", "R2"];
let direccion = ["norte", "este", "sur", "oeste"];
let medidaX = 0; //cuando se mueve al este suma y cuando se mueve al oeste resta
let medidaY = 0; //cuando se mueve al norte suma y cuando se mueve al sur resta

function girar(input) {
  switch (direccion) {
    case "norte":
      if ((input = R)) {
        direccion = "este";
        caminar(direccion);
      } else {
        false;
      }
      break;
    case "este":
      break;
    case "sur":
      break;
    case "oeste":
      break;
  }
}

caminar(direccion){
  switch
}
